{
  "buildCommand": "cd packages/shared && npx tsc && cd ../mahjong-engine && npx tsc && cd ../../apps/mobile && npx expo export --platform web && node scripts/inject-landscape-lock.js",
  "outputDirectory": "apps/mobile/dist",
  "installCommand": "npm install -g pnpm && pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
